export enum config {
  // 端口
  port = 30000,

  // 根目录
  prefix = '/node',

  // 接收的数据格式
  contentType = 'application/json;charset=UTF-8'
}

export enum pundix {
  appId = '',
  appSecret = '',
}

export enum errorCode {
  'Not Found this Api' = 404
}